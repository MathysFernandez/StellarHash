[package]
name = "StellarHash"
version = "0.1.0"
edition = "2024"

[dependencies]
# On importe Bevy
bevy = "0.14"

# On active les optimisations au maximum pour la version finale
[profile.release]
opt-level = 3
lto = "thin"